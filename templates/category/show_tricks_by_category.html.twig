{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}


{% block body %}
    <div class="list-tricks">
        {% for trick in tricks %}
            {% if trick.categoryId.id== category.id %}
                {% include 'trick/_trick_cards.html.twig' %}
            {% endif %}
        {% endfor %}
    </div>
{% endblock %}